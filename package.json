{
  "name": "dotmatter-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.13.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm --filter @dotmatter/core build && pnpm --filter @dotmatter/shaders build && pnpm --filter @dotmatter/react build && pnpm -r typecheck"
  },
  "devDependencies": {
    "playwright": "1.61.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
